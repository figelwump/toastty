interface:
  display_name: "Toastty Worktree Create"
  short_description: "Start or fork a task into its Toastty worktree"
  default_prompt: "Use $worktree-create to create a task workspace and worktree, then start the established task. Plan only if I explicitly requested a read-only outcome."
