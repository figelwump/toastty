interface:
  display_name: "Worktree Done"
  short_description: "Accept this worktree's PR and merge it when checks pass"
  default_prompt: "Use $worktree-done: I have finished reviewing this version and want its PR merged when the required checks pass."
