{
  "name": "@toastty/diagnostics-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types --env test",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run types && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.16.20",
    "typescript": "6.0.3",
    "vitest": "4.1.9",
    "wrangler": "4.105.0"
  }
}
